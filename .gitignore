# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store

# `.env` is intentionally COMMITTED: it holds non-secret defaults so the app
# runs with zero setup. Real keys belong in `.env.local`, which is ignored.
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OMC / agent tooling state
.omc/

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/
